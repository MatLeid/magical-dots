language: android

android:
  components:
    - platform-tools
    - tools
    - build-tools-21.0.0
    - android-21

branches:
  only:
    - master

before_install:
    - chmod +x gradlew
    - git config --global user.email "ci@donmatheo.de"
    - git config --global user.name "Travis CI"
    - git config --global push.default simple

script:
    - ./gradlew build html:dist

after_failure:
    - cd ..
    - git clone -b gh-pages --single-branch https://github.com/donmatheo/unstable-relations.git gh-pages
    - cd gh-pages
    - cp -r ../unstable-relations/core/build/reports/tests/ tests
    - git add tests

after_success:
    - cd ..
    - git clone -b gh-pages --single-branch https://github.com/donmatheo/unstable-relations.git gh-pages
    - cd gh-pages
    - mv latest_build archive/build_$((TRAVIS_JOB_ID - 2))
    - cp -r ../unstable-relations/html/build/dist/ latest_build
    - echo "$((TRAVIS_JOB_ID - 1))" >> latest_build/version.txt
    - git add latest_build
    - git add archive

after_script:
    - git commit -a -m "Committed by Travis-CI (build number - $((TRAVIS_JOB_ID - 1)))"
    - git pull
    - git push -q https://$token@github.com/donmatheo/unstable-relations.git HEAD:gh-pages